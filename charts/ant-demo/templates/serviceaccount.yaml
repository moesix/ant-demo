apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ant-demo.serviceAccountName" . }}
  labels:
    {{- include "ant-demo.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}