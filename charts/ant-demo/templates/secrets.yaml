apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ant-demo.fullname" . }}-secrets
  labels:
    {{- include "ant-demo.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}